// Wire-aware opt-in marker. Its presence tells a consuming target's build plugin to
// re-parse this library's sources — picking up the handlers `CollectedKey` and the
// `WireGraphConformanceV1` declaration. Presence-only.
